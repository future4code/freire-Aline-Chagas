export const goToTripsList = (navigate) => {
  navigate("/trips/list");
};

export const LoginAdmin = (navigate) => {
  navigate("/login");
};

export const Return = (navigate) => {
    navigate(-1)
}